
CREATE SCHEMA IF NOT EXISTS mlflow AUTHORIZATION ${POSTGRES_USER};


ALTER DATABASE ${POSTGRES_DB} SET search_path TO mlflow, public;


ALTER ROLE ${POSTGRES_USER} IN DATABASE ${POSTGRES_DB} SET search_path TO mlflow, public;
